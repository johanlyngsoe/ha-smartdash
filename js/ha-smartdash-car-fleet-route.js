(function(){
  const F=window.BeastCarFleet;if(!F)return;let wired=false;
  function css(){if(document.getElementById("beastCarFleetRouteStyles"))return;const s=document.createElement("style");s.id="beastCarFleetRouteStyles";s.textContent=`.beast-car-fleet-route{display:flex;flex-wrap:wrap;gap:8px;margin:0 0 10px}.beast-car-fleet-route span{padding:6px 9px;border:1px solid var(--border);border-radius:999px;background:var(--surface-solid);color:var(--ink-muted);font-size:.78rem;font-weight:700}.beast-car-fleet-route strong{color:var(--ink)}`;document.head.appendChild(s);}
  function data(i){const r=F.state(i.route),d=Number(F.state(i.distance)?.state),ee=F.state(i.eta),known=r&&!['','unknown','unavailable'].includes(String(r.state).toLowerCase()),hasD=Number.isFinite(d);let eta="";if(ee&&!['unknown','unavailable'].includes(String(ee.state).toLowerCase())){const x=new Date(ee.state);if(Number.isFinite(x.getTime()))eta=x.toLocaleTimeString("da-DK",{hour:"2-digit",minute:"2-digit"});}return{active:known||hasD||!!eta,distance:hasD?d:null,eta,key:`${r?.state||""}:${hasD?d.toFixed(1):""}:${eta}`};}
  function apply(){const map=document.querySelector("[data-car-map='1']");if(!map)return;const i=F.ids();if(!wired){wired=true;css();F.watch(apply,[i.route,i.distance,i.eta]);}const x=data(i),old=map.querySelector("[data-fleet-route]");if(!x.active){old?.remove();return;}if(old?.dataset.fleetRouteKey===x.key)return;const parts=[];if(x.distance!==null)parts.push(`<span>Til mål <strong>${x.distance.toFixed(1)} km</strong></span>`);if(x.eta)parts.push(`<span>Ankomst <strong>${F.escapeHtml(x.eta)}</strong></span>`);if(!parts.length)parts.push(`<span><strong>Rute aktiv</strong></span>`);const wrap=document.createElement("div");wrap.className="beast-car-fleet-route";wrap.dataset.fleetRoute="1";wrap.dataset.fleetRouteKey=x.key;wrap.innerHTML=parts.join("");if(old)old.replaceWith(wrap);else map.querySelector(".beast-car-v3-map-head")?.insertAdjacentElement("afterend",wrap);}
  F.watch(apply,[]);
})();
